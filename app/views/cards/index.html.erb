<div class="main">
  <div class="container">
  <h2>Все карточки</h2>
    <% cards = Card.all %>
    <% @cards.each do |card| %>
    <div class="card">
      <%#= image_tag card.image %>
      <h3><%= card.original_text %></h3>
      <p><%= card.translated_text %></p>
      <p><%= card.review_date %></p>
      <p><%= link_to "Открыть", card_path(card) %>
    </div>
    <% end %>
  </div>
</div>
